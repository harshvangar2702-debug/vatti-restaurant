# Gmail Configuration for Password Reset Emails
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Database Configuration (optional - if using MongoDB Atlas)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/restauraant

# JWT Secret (optional - currently hardcoded as 'your_jwt_secret')
# JWT_SECRET=your_jwt_secret_here
